# Local PostgreSQL example for development
# Keep real Supabase credentials only in .env or .env.local
DATABASE_URL="postgresql://skillspoints:skillspoints_dev@localhost:5432/skillspoints?schema=public"
# Configuration de la base de données locale
POSTGRES_DB=skillspoints
POSTGRES_USER=skillspoints
POSTGRES_PASSWORD=skillspoints_dev